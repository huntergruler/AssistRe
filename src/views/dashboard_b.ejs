<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="header.css">
    <link rel="stylesheet" href="styles.css">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <title>Dashboard</title>
</head>
<header>
    <div class="row row-cols-2">
        <div id="title" class="align-items-center">
            <h1 class="mt-2"><a href="/">SeekingAgents</a></h1>
        </div>
        <nav id="navlinks" class="justify-content-end text-center">
            <p id="navname">
                <%= firstname %>
                    <%= lastname %>
            </p>
            <p>|</p>
            <p><a href="/profile_b">Profile</a></p>
            <p>|</p>
            <p><a href="/settings">Settings</a></p>
            <p>|</p>
            <p><a href="/logout">Logout</a></p>
        </nav>
    </div>

</header>

<body id="dashboardBody">
    <div id="dashboardContainer">
        <h1>Buyer Dashboard for <%= firstname %>
                <%= lastname %>
        </h1>
    </div>
    <div class="container mt-5">
        <table class="table">
            <thead>
                <tr>
                    <th>Bathrooms Min</th>
                    <th>Bedrooms Min</th>
                    <th>Buyer Type</th>
                    <th>Preferred Languages</th>
                    <th>Prequalified</th>
                    <th>Price Min</th>
                    <th>Price Max</th>
                    <th>Property Type</th>
                    <th>Square Footage Min</th>
                    <th>Square Footage Max</th>
                    <th>Time Frame</th>
                    <th>Entry Timestamp</th>
                    <!-- Add more headers as needed -->
                </tr>
            </thead>
            <tbody>
                {% for row in data %}
                <tr>
                    <td>{{ row.bathrooms_min }}</td>
                    <td>{{ row.bedrooms_min }}</td>
                    <td>{{ row.buyerType }}</td>
                    <td>{{ row.preferredLanguages }}</td>
                    <td>{{ row.prequalified }}</td>
                    <td>{{ row.price_min }}</td>
                    <td>{{ row.price_max }}</td>
                    <td>{{ row.propertyType }}</td>
                    <td>{{ row.squareFootage_min }}</td>
                    <td>{{ row.squareFootage_max }}</td>
                    <td>{{ row.timeFrame }}</td>
                    <td>{{ row.entrytimestamp }}</td>
                    <!-- Add more cells as needed -->
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>

</html>

agentid
buyerid
bathrooms_min
bedrooms_min
buyerType
city
preferredLanguages
prequalified
price_min
price_max
propertyType
squareFootage_min
squareFootage_max
state
timeFrame
entrytimestamp
zipCodes