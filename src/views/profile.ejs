<!DOCTYPE html>
<html lang="en">
    <head>
        <link rel="stylesheet" href="/styles.css"> <!-- Link to your external CSS file -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Ensure you have some CSS for basic styling -->
    <link rel="stylesheet" href="header.css">
</head>
<header>
    <h1 id="title"><a href="/">SeekingAgents</a></h1>
    <div id="navlinks">
        <a href="/dashboard">Dashboard</a>
        <p>|</p>
        <a href="/settings">Settings</a>
        <p>|</p>
        <a href="/logout">Logout</a>
    </div>
</header>
<body>
    <div id="parentElement">
    <div id="profileContainer">
        <h1>Profile Page</h1>
        <br>
        <p><b>Current Licenses</b></p>
    <table id="licensesTable">
        <thead>
            <tr>
                <th>License State</th>
                <th>License Number</th>
                <th>Expiration Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% if (hasLicenses) { %>
              <% licenses.forEach(function(license) { %>
                <tr id="license-<%= license.agentlicenseid %>">
                    <td><%= license.licenseState %></td>
                    <td><%= license.licenseNumber %></td>
                    <td><%= license.licenseExpirationDate %></td>
                    <td>
                        <button type="button" onclick="deleteLicense(<%= license.agentlicenseid %>)">Delete</button>
                    </td>
                </tr>
            <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="3">No licenses found.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <form id="licenseForm">
        <input type="text" id="licenseState" name="licenseState" placeholder="License State" maxlength="2" minlength="2" oninput="this.value = this.value.toUpperCase()" required pattern="[A-Z]{2}" title="Enter a valid US state abbreviation">
        <input type="text" id="licenseNumber" name="licenseNumber" placeholder="License Number" required>
        <!-- <input type="date" id="licenseExpirationDate" name="licenseExpirationDate" placeholder="License Expiration Date" required> -->
        <label for="monthSelect">Month:</label>
        <select id="monthSelect" onchange="updateDays();">
            <!-- Options will be generated by JavaScript -->
        </select>
        
        <label for="daySelect">Day:</label>
        <select id="daySelect">
            <!-- Options will be generated by JavaScript -->
        </select>
        
        <label for="yearSelect">Year:</label>
        <select id="yearSelect" onchange="updateDays();">
            <!-- Options will be generated by JavaScript -->
        </select>
                <button type="button" id="licenseAdd" disabled onclick="addLicense()">Add</button>
        <span id="licStatus"></span>
    </form>
</div>
<br>
<p><b>Zip Codes </b></p>
<p> Select the Zip Codes you are want to make offers in</p>
<p>Limit zip codes by state and city if desired</p>
<div id="zipCodeParam">
    <label for="stateSelect">States:</label>
    <select id="stateSelect" onchange="populateCities();">
    </select>   
    <label for="citySelect">Cities:</label>
    <select id="citySelect">
    </select>
    <button type="button" id="zipUpdate" disabled onclick="zipUpdate()">Update zip code list</button>
    <div class="container">
        <p>Available Zip Codes</p>
        <div id="availabeZipCodesContainer" class="box1">
        <!-- JavaScript will populate this container -->
    </div>
    <p>Selected Zip Codes</p>
    <div id="availabeZipCodesContainer" class="box2">
        <!-- JavaScript will populate this container -->
    </div>
</div>
</div>

</div>
</div>
<script src="/profile.js"></script>
</body>
</html>
