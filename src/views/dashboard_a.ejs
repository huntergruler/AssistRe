<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agent Dashboard</title>
    <link rel="stylesheet" href="header.css">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="dashboard_a.css">
</head>
<header>
    <div class="row row-cols-2">
        <div id="title" class="align-items-center">
            <h1 class="mt-2"><a href="/">SeekingAgents</a></h1>
        </div>
        <nav id="navlinks" class="justify-content-end text-center">
            <p id="navname">
                <%= agent.firstname %>
                    <%= agent.lastname %>
            </p>
            <p>|</p>
            <p><a href="/profile_a">Profile</a></p>
            <p>|</p>
            <p><a href="/settings">Settings</a></p>
            <p>|</p>
            <p><a href="/logout">Logout</a></p>
        </nav>
    </div>

</header>

<body id="dashboardBody">
    <div class="margin-top-100"></div>
    <div id="dashboardContainer" style="text-align: center;">
        <h2>Agent Dashboard for <%= agent.firstname %>
                <%= agent.lastname %>
        </h2>
    </div>
    <br>
    <div class="container-left mt-5">
        <div class="form-row col-md-4">
            <div class="form-group row justify-content-center">
                <% for (let row of data) { %>
                        <%= row.propertyType %> residence <br>
in the next <%= row.timeFrame %><br> $<%= row.price_min %> to $<%= row.price_max %> 
                        <br><% if (row.prequalified == 1) { %>
                            Pre-qualified
                        <% } else { %>
                            NOT Pre-qualified
                        <% } %>
                <% } %>
                </div>
            </div>
        </div>
    </div>
    <table class="table">
            <!-- <thead>
                <tr style="text-align: center;">
                    <th>Property Type</th>
                    <th>Time Frame</th>
                    <th>Price Min</th>
                    <th>Price Max</th>
                    <th>Pre qualified</th>
                    <th>Buyer Type</th>
                    <th>Square Foot Min</th>
                    <th>Square Foot Max</th>
                    <th>Entry Timestamp</th>
                </tr>
            </thead> -->
            <tbody>
                <% for (let row of data) { %>
                    <tr style="text-align: center;">
                        <td>
                            <%= row.propertyType %>
                        </td>
                        <td>
                            <%= row.timeFrame %>
                        </td>
                        <td>
                            <%= row.price_min %>
                        </td>
                        <td>
                            <%= row.price_max %>
                        </td>
                        <td>
                            <%= row.prequalified %>
                        </td>
                        <td>
                            <%= row.buyerType %>
                        </td>
                        <td>
                            <%= row.squareFootage_min %>
                        </td>
                        <td>
                            <%= row.squareFootage_max %>
                        </td>
                        <td>
                            <%= row.entrytimestamp %>
                        </td>
                        <!-- Add more cells as needed -->
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
</body>

</html>